// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    apply from: "config.gradle"
    addRepos(repositories)

    dependencies {
        classpath deps.android_build_gradle
        classpath deps.and_res_guard_gradle_plugin
        classpath deps.objectbobx_plugin
        classpath deps.dagger.dagger_hilt_plugin
        classpath deps.kotlin_gradle_plugin
        classpath deps.aspectjx_plugin
        classpath deps.arouter.register
        classpath deps.fat_aar

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }

//    经过验证，加上统一版本号后，编译和打包时间大致一样，有时候加上统一版本快一点点，有时候不加快一点点，可以先不加
//    subprojects {
//        project.configurations.all {
//            resolutionStrategy.eachDependency { details ->
//                if (details.requested.group == 'androidx.activity') {
//                    details.useVersion "1.2.3"
//                }
//                if (details.requested.group == 'androidx.annotation') {
//                    details.useVersion "1.3.0"
//                }
//                if (details.requested.group == 'androidx.appcompat') {
//                    details.useVersion "1.3.0"
//                }
//                if (details.requested.group == 'androidx.arch.core' && (details.requested.name.contains('common') || details.requested.name.contains('runtime'))) {
//                    details.useVersion "2.1.0"
//                }
//                if (details.requested.group == 'androidx.collection') {
//                    details.useVersion "1.1.0"
//                }
//                if (details.requested.group == 'androidx.constrintlayout') {
//                    details.useVersion "1.1.0"
//                }
//                if (details.requested.group == 'androidx.core') {
//                    details.useVersion "1.5.0"
//                }
//                if (details.requested.group == 'androidx.customview') {
//                    details.useVersion "1.1.0"
//                }
//                if (details.requested.group == 'androidx.lifecycle'
//                        && !details.requested.name.contains('extensions')) {
//                    details.useVersion "2.3.1"
//                }
//                if (details.requested.group == 'androidx.databinding') {
//                    details.useVersion "4.2.2"
//                }
//            }
//        }
//    }
}


allprojects {
    repositories {
        addRepos(repositories)
        google()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
